SELECT
                                            s.Id,
                                             s.FirstName, s.LastName FROM Student s WHERE s.CohortId = 1
                                            